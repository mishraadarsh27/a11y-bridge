[tool.black]
line-length = 88
target-version = ["py311"]

[tool.ruff]
line-length = 88
target-version = "py311"
extend-exclude = [
  ".venv",
  "models",
  "data",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
